//习题6.5-8,用k个已排序链表合并为一个排序链表的算法（用一个最小堆来做k路合并）
//1、将k个链表的第一个元素取出组成一个最小堆，原链表中的第一个元素删除
//2、堆顶元素a[1]取出放入输出链表中
//3、若拿出的元素所在的原链表不为空，取出第一个元素放入堆顶，然后max_heapify(a,1)并删除原链表中的该元素，返回2
//4、若为空，交换a[1]与a[heap_size]，heap_size--,然后max_heapify(a,1)，返回2
