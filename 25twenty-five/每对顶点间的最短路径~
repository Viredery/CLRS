邻接矩阵：
             0                   如果i=j
    wij  =  {有向边(i,j)的权     如果i!=j且(i,j)属于E      }
             无穷大              如果i!=j且(i,j)不属于E 
允许存在权值为负的边

输出的是一个前趋矩阵。对于第i行，导出的子图是以i为根的一棵最短路径树


最短路径与矩阵乘法：
    基于矩阵乘法的动态规划算法。采用“重复平方”技术。O(V3lgV) 邻接矩阵

Floyd-Warshall算法：
    动态规划 邻接矩阵
    算法允许存在权值为负的边，但假设不存在权值为负的回路
    dij(k)为从顶点i到顶点j、且满足所有中间顶点皆属于集合{1,2,...,k}的一条最短路径的权值。  dij(0)=wij
        D(n)=(dij(n))即为最终解答
    O(V3)
    应用：
        G的传递闭包定义为图G*=(V,E*)，其中E*={(i,j):图G中存在一条从i到j的路径}
        把Floyd-Warshall算法中的min和+算术运算操作，用相应的逻辑运算∨和∧来代替，降低了空间复杂度

稀疏图上的Johnson算法：
    邻接表 重赋权技术
    将Dijkstra和Bellman-Ford算法作为其子程序
    新的w`(u,v) = w(u,v)+h(u)-h(v)
